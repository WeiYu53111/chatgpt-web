export interface UserData {
  email: string;
  create_time: string;
  last_login_time: string;
  usage: number;
  limit: number;
}

export interface UserHisData {
  squad_date: string;
  email: string;
  usage: number;
}
